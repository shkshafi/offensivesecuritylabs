<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Avoid infinite loop by checking if request is already for public/
    RewriteCond %{REQUEST_URI} !^/public/
    
    # Rewrite all requests to the public/ folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
